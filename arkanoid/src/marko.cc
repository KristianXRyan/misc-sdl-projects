#include "marko.hh"

namespace ark
{
    
    MenuObject::MenuObject(int x, int y, int w, int h)
        : ArkObject(x, y, w, h)
    {
    
    }
    
    void MenuObject::drawTo(SDL_Renderer *canvas) const
    {
    
    }
}